# Digital Museum — Configuration
# This file was created automatically on first run.
# Edit it to configure your instance, then restart the app.
# Location: %APPDATA%\digital-museum\.env

# ── SQLite ────────────────────────────────────────────────────────────────────
# BILLING_SQLITE_PATH is required: it stores archive_profiles (which archive opens at startup).
# Main archive files are not read from SQLITE_PATH; set a startup default under Admin → Archives.
# Dev: set BILLING_SQLITE_PATH in this file or project-root .env.
# BILLING_SQLITE_PATH=C:/path/to/billing.sqlite

# ── Deployment ───────────────────────────────────────────────────────────────
DEPLOYMENT_NATURE=local
PAGE_TITLE=Digital Museum of SUBJECT_NAME

# ── Admin account (used on first startup to create the admin user) ───────────
# Password must be at least 12 characters (argon2id policy).
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMeOnFirstRun

# ── AI Provider Keys (at least one required) ─────────────────────────────────
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...
# TAVILY_API_KEY=...

# ── Optional AI model overrides ──────────────────────────────────────────────
# CLAUDE_MODEL_NAME=claude-sonnet-4-6
# GEMINI_MODEL_NAME=gemini-2.5-flash
